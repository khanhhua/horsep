on:
  pull_request:

jobs:
  unit-test:
    name: Unit Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-haskell@v1.1
        with:
          ghc-version: latest
          cabal-version: latest
      - run: cabal v2-run horsep-test